{% extends 'base.html.twig' %}

{% block title %}Ligue-R3 Poule F{% endblock %}

{% block body %}

<ol class="breadcrumb">
    <li class="breadcrumb-item"><a href="{{ path('accueil') }}">Accueil</a></li>
    <li class="breadcrumb-item active">Ligue-R3 Poule F</li>
</ol>
<main>
    <h1><center>Classement Regionale 3 - Poule F</center></h1>
    <div class="table-responsive">
        <table class="table table-bordered" id="classR1">
            <thead class="thead-dark">
                {% for tableth in tablesth %}
                        <th scope="col">{{ tableth }}</th>
                {% endfor %}
            </thead>
            <tbody>
            {% for table in tables %}
                <tr>
                    {% for value in table %}
                        <td>{{ value|replace({'J.S BASSIN AVEYRON':'J.S BASSIN AVEYRON <img src="https://android-apiapp.azureedge.net/common/bib_img/logo/550055.jpg" style="height:25px; width:auto;">'})|replace({'ONET LE CHAT. FOOT 2':'ONET LE CHAT. FOOT 2 <img src="https://android-apiapp.azureedge.net/common/bib_img/logo/525743.jpg" style="height:25px; width:auto;">'})|replace({'SEGALA RIEUPEYROUX':'SEGALA RIEUPEYROUX <img src="https://android-apiapp.azureedge.net/common/bib_img/logo/549427.jpg" style="height:25px; width:auto;">'})|raw }}</td>
                    {% endfor %}
                </tr>
            {% endfor %}
            </tbody>
        </table>
        {# <div>Les descentes sont actualisées en fonction des descentes de Régionale 3.</div><br><br> #}
    </div>
    <h1><center>Résultats Dernière Journée</center></h1>
    <div id="resultat">
        {% for value in div %}
            <div>{{ value|replace({'data-src':'src'})|replace({'href="':'target="_blank" href="https://occitanie.fff.fr/'})|replace({'<div class="date_tour">':'<span class="date_tour">'})|raw }}</div>
        {% endfor %}
    </div>
</main>
{% endblock %}