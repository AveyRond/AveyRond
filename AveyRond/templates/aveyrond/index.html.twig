
{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}

<main id="main_index">
    {# <h1>Prochain Match :</h1>
    <table class="table table-hover" data-page-length="5">
        <thead class="thead-dark">
            <tr>
                <th scope="col" class="align-middle">Equipe</th>
                <th scope="col" class="align-middle">Equipe Adverse</th>
                <th scope="col" class="align-middle">Date et Heure</th>
            </tr>
        </thead>
        <tbody>  
        {% if matchs is defined %}
        {% for match in matchs %}
            <tr>
                <td>{{ match.Equipe }}</td>
                <td>{{ match.Adversaire }}</td>
                <td>{{ match.DateMatch | date('d/m/Y H:i') }}</td>
            </tr>
        {% endfor %}
        {% endif %}
        </tbody>
    </table> #}
    
    <section class="articles">

        {% for article in articles %}
            <article id="index">
                <img src={{ article.image }} onclick=location.href="{{ path('show_article', {'id': article.id}) }}">
                <div>
                    <span class="badge badge-dark">{{ article.category.categoryType }}</span>
                    <h1 onclick=location.href="{{ path('show_article', {'id': article.id}) }}">{{ article.title }}</h1>
                    {# <p class="content">{{ article.content | striptags | u.truncate(80, '...', false) | raw }}</p> #}
                    {# <a href="{{ path('show_article', {'id': article.id}) }}" class="btn btn-outline-primary">Lire la suite</a> #}
                    <hr>
                </div>
                {# <div></div> #}
            </article> 
        {% endfor %}
        {{ knp_pagination_render(articles) }}

    </section>
</main>
{% endblock %}
